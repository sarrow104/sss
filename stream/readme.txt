* readme.txt

date:2015-07-03

======================================================================

鉴于编写解析器的情况较多，我应当将字符串分析、判断的部分抽线隔离开来。

比如：

另外，我需要设定其行为：

是否抛出异常！
是否避开空白字符？

class skipper

skip_if_nothrow()

skip_if()

skip_if_set()
skip_if_set_verse()

skip_if_dec()
skip_if_oct()
skip_if_hex()
skip_if_bin()

----------------------------------------------------------------------

另外，还需要返回消耗掉的字符信息。

这些信息，我觉得可以从skipper对象中获取；

这样思考的话，是否抛出异常，可以从提供给方法调用的参数数量来决定！

----------------------------------------------------------------------

另外，BDEAlias 模块下，还有 read_xxxx 系列函数，可以提取出来公用；

----------------------------------------------------------------------

date:2015-08-16

streamu/tf8.hpp

增加针对utf8的过滤器、抽取器。

其行为与 skipper.hpp 类似；不过，是专门针对utf8字符流的。

一些假设：

1. 一个byte长的，显示宽度，= 1em；
2. 两个byte长的，显示宽度，= 1em;
3. 三个byte长的，显示宽度，= 2em；

----------------------------------------------------------------------
bool validate(s_beg, s_end); // 检查
