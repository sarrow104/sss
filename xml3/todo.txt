* todo

======================================================================

xml-selector

----------------------------------------------------------------------

Selector sel("path");
sel.find(myXmlDoc, nodeHandle);

while (sel.fetch()) {
   nodeHandle.print()
}

std::vector<node*> node_list;
while (sel.fetch()) {
   node_list.push_back(nodeHandle.get());
}

----------------------------------------------------------------------

1. 应当将 node_list_t和 Selection统和在一起；

2. 允许一个一个地，将符合条件的节点，检索出来；
   而不是一次性；

----------------------------------------------------------------------

** iconv的时机

还是说

关键在于，编码的内存表示与文件编码的异同。

内存表示，肯定需要与其他的字符串进行交互；对于linux来说，utf8，肯定是上策；

而windows下来说，需要根据系统的codepage来决定，对于中文系统来说，也就是cp936；

----------------------------------------------------------------------

至于外部的编码，只与<? xml encoding="" ?> 的设置有关！
